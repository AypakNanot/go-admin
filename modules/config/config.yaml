---
database:
  default:
    file: "./data/admin.db"
    max_idle_con: 50
    max_open_con: 150
    driver: sqlite
domain: localhost
prefix: admin
store:
  path: "./uploads"
  prefix: uploads
language: en
index: "/"
debug: true
open_admin_api: true
color_scheme: skin-black